# Slack user token — NEVER commit. Live xoxp secret used for local testing.
.slack_token

# AI session handover notes: local only, never commit (internal context)
HANDOVER.md
HANDOVER-BUILD.md

# Node / build
node_modules/
dist/
*.tsbuildinfo

# Coverage reports — generated by `pnpm -r test:coverage`, never commit
coverage/

# Local resolver cache (name->id) — never commit
.cache/
*.cache.json

# Loop/demo scratch
research/loop.gen

# pnpm content-addressable store — never commit
.pnpm-store/
